* `CONFIG_DIR` - not required, path to directory with network config
* `CONFIG_PATH` - path to network config
* `START_INDEX` - index of the first row to train
* `END_INDEX` - index of the last row to train
* `LOG_TO_FILES` - if `1` then log info about synapses to the files
